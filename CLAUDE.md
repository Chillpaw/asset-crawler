# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`asset-crawler` is a Python project (early stage — no source code yet). The `.gitignore` includes patterns for common Python tooling: pytest, ruff, mypy, uv/pipenv/poetry/pdm, venv, and Streamlit/Marimo notebooks.

## Setup & Commands

This project has no build system or dependencies defined yet. As the project grows, add the relevant commands here (e.g., `uv sync`, `pytest`, `ruff check .`).
